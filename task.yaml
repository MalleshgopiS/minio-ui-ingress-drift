id: minio-ui-ingress-drift
prompt: |
  The MinIO UI ingress named `bleater-ui` in the `bleater`
  namespace has configuration drift.
  ...
metadata:
  category: kubernetes
  difficulty: easy

evaluation:
  runs: 5             # Match the number of runs required by your spec
  model: smallie      # Change this from 'starburst-plus' or 'biggie-plus' to 'smallie'